{
  "name": "chaika",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm-run-all --parallel dev-ip-sync browser-sync",
    "dev-ip-sync": "dev-ip",
    "browser-sync": "browser-sync start --server --files 'index.html, css/*.css, js/*.js, img/*.*'"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "browser-sync": "^3.0.2",
    "dev-ip": "^1.0.1",
    "npm-run-all": "^4.1.5"
  }
}
